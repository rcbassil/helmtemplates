global: {}

# -- Number of pod replicas
replicaCount: 1

image:
  # -- Container image repository
  repository: app-beta
  # -- Image pull policy
  pullPolicy: IfNotPresent
  # -- Image tag. Defaults to the chart appVersion when empty
  tag: ""

# -- Override the chart name
nameOverride: ""
# -- Override the full release name
fullnameOverride: ""

serviceAccount:
  # -- Create a ServiceAccount for the pod
  create: true
  # -- Automount the ServiceAccount API token
  automount: true
  # -- Annotations to add to the ServiceAccount
  annotations: {}
  # -- Name of the ServiceAccount. Generated from the fullname template when empty
  name: ""

# -- Annotations to add to the pod
podAnnotations: {}
# -- Labels to add to the pod
podLabels: {}

# -- Pod-level security context. Inherits from global.podSecurityContext when empty
podSecurityContext: {}

# -- Container-level security context. Inherits from global.securityContext when empty
securityContext: {}

service:
  # -- Kubernetes Service type
  type: ClusterIP
  # -- Service port
  port: 9090

# -- Container resource requests and limits
resources: {}

# -- Liveness probe configuration
livenessProbe:
  httpGet:
    path: /health
    port: http

# -- Readiness probe configuration
readinessProbe:
  httpGet:
    path: /health
    port: http

autoscaling:
  # -- Enable HorizontalPodAutoscaler
  enabled: false
  # -- Minimum number of replicas
  minReplicas: 1
  # -- Maximum number of replicas
  maxReplicas: 10
  # -- Target CPU utilization percentage
  targetCPUUtilizationPercentage: 80

# -- Additional volumes
volumes: []
# -- Additional volume mounts
volumeMounts: []

# -- Node selector for pod scheduling
nodeSelector: {}
# -- Tolerations for pod scheduling
tolerations: []
# -- Affinity rules for pod scheduling
affinity: {}
